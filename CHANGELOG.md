# Arch Linux Tweak Tool — Changelog

| Date yyyy.mm.dd | Description | Additional remarks | Relates to |
|---|---|---|---|
| 2026.04.26 | **Software menu enhanced** with standardized installation flow across all package managers. Install buttons now check if package is already installed and display notifications. All GUI package managers (Pamac, Octopi, GNOME Software, KDE Discover, Bauh) and TUI tools (Pacseek, Pacui) auto-launch after installation. Remove buttons now preserve "Remove" label. Terminal output includes verbose [INFO] logging for installation/removal process: waiting, completion, binary check, and launch status. Labels show **installed** suffix only after successful installation. pacseek and pacui now have install+launch logic like other tools. gparted now has install+launch logic with logging. discover installation now includes packagekit-qt6. | AUR helpers, Flatpak, Snapd, App-manager also updated with verbose logging and pre-check for already-installed packages. Missing repository notifications show specific repo names (nemesis_repo, chaotic-aur). Removal process logs final completion message. | software_gui.py — archlinux-tweak-tool.py — functions.py — system_gui.py |
| 2026.04.21 | New **Network** menu added to the left sidebar. Contains two horizontal tabs: **Network** (network discovery, nsswitch.conf) and **Samba** (samba server setup, config, user creation, file manager plugins). Both sections moved out of the Services page. | Services page now has Audio, Bluetooth, Printing only | network_gui.py — services_gui.py |
| 2026.04.21 | New **Software** menu added to the left sidebar. Four sections: GUI Package Managers (Pamac, Octopi, GNOME Software, KDE Discover, Bauh), AUR Helpers (yay-git, paru-git, trizen, pikaur-git), Flatpak / Snap / AppImage, and TUI Package Tools (Pacseek, Pacui). Labels show **installed** when the binary is detected. | GNOME apps (gnome-software, app-manager) require HOME + XDG\_RUNTIME\_DIR + DBUS\_SESSION\_BUS\_ADDRESS when launched from root via sudo | software_gui.py — archlinux-tweak-tool.py |
| 2026.04.21 | AUR-only packages (snapd, app-manager) detect the available AUR helper (yay → paru → trizen → pikaur) and install via alacritty. Shows an in-app notification if no AUR helper is found. | Flatpak and Snapd terminal output improved with usage hints | software_gui.py — archlinux-tweak-tool.py |
| 2026.04.19 | **Kernel Manager** gains systemd-boot default boot entry selection. Dropdown lists available entries; applying sets the default. Uses in-app notification instead of dialog for feedback. | Notification label padding fixed for proper display | kernel_gui.py — archlinux-tweak-tool.py |
| 2026.04.18 | **AUR Helper** support added to the Pacman page: builds yay-git or paru-git from the AUR using `makepkg` as the real user (`fn.sudo_username`). AUR helper buttons renamed to yay-git / paru-git and button logic fixed. | chaotic-aur support also added; `pacman_functions.py` restored | pacman_gui.py — archlinux-tweak-tool.py |
| 2026.04.15 | **Fixes page renamed to Maintenance**. New features added: Update system, Clean cache, Clear orphans, Get best servers, Set mainstream servers, Install apps, Remove pacman lock. Terminal windows now show a closing message. Layout rearranged for logical order. | | maintenance_gui.py — archlinux-tweak-tool.py |
